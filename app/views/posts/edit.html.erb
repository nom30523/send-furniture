<div class="container">
  <div class="new-post-form">
    <%= form_with(model: @post, local: true) do |form|%>
      <h3>編集する</h3>
      <%= render partial: "form", locals: { form: form } %>
    <% end %>
  </div>
</div>